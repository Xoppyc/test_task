info:
  name: Get All Users copy
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:5050/api/users/users/?id=0
  params:
    - name: id
      value: "0"
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
