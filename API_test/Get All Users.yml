info:
  name: Get All Users
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5050/api/users/users/
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
