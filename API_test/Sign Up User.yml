info:
  name: Sign Up User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5050/api/auth/signup
  body:
    type: json
    data: |-
      {
        "name": "John",
        "email": "john@test.com",
        "password": "123456"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: bru.setVar('token', res.body.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
